CheckBox:

```js
    <CheckBox
        size={25}
        isEnabled={true}
        isChecked={false}
        isReadOnly={false}
        onToggle={(input) => {console.log(input)}} />
```
```js
    <CheckBox
        size={25}
        title={'Checkbox Title'}
        showTitle
        isEnabled={true}
        isChecked={false}
        isReadOnly={false}
        onToggle={(input) => {console.log(input)}} />
```
